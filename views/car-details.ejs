<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Car Details</title>
    </head>
    <body>
        <!-- Include the header for navigation -->
        <%- include('partials/header') %>

        <h1>Car Details</h1>

        <!-- Display the details of the car -->
        <ul>
            <li><strong>Make:</strong> <%= car.make_name %></li>
            <li><strong>Model:</strong> <%= car.model_name %></li>
            <li><strong>Vehicle Type:</strong> <%= car.vehicle_type_name %></li>
            <li><strong>Year:</strong> <%= car.yr %></li>
            <li><strong>License Plate:</strong> <%= car.license_plate %></li>
            <li><strong>Mileage:</strong> <%= car.mileage %> km</li>
            <li><strong>Status:</strong> <%= car.status %></li>
        </ul>

        <!-- Edit Car Button -->
        <a href="/cars/<%= car.car_id %>/edit">
            <button>Edit Car</button>
        </a>

        <!-- Delete Car Button -->
        <form
            action="/cars/<%= car.car_id %>?_method=DELETE"
            method="POST"
            style="display: inline"
        >
            <input
                type="password"
                name="password"
                placeholder="Enter password"
                required
            />
            <button
                type="submit"
                onclick="return confirm('Are you sure you want to delete this car?');"
            >
                Delete Car
            </button>
        </form>

        <!-- Link to go back to the list of cars -->
        <p><a href="/cars">Back to Cars</a></p>
    </body>
</html>
